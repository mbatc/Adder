
let test_result: int64 = 0;

fn return_first(val1: int64, val2: int64) => int64 {
  return val1;
}

fn return_second(val1: int64, val2: int64) => int64 {
  return val2;
}

fn main() => void {
  const first: int64 = 2;
  const second: int64 = 3;

  const equals_second: int64 = return_second(first + second, second + 10);
  const equals_first: int64 = return_first(first + second, second);

  test_result = equals_second;
  test_result = equals_first;
}
